# Production: paste the Supabase Session pooler URI and keep it secret.
DATABASE_URL=
# Local fallback used only when DATABASE_URL is empty.
VERTICE_DATABASE_PATH=./data/vertice.db
DASHBOARD_ORIGIN=http://localhost:3000
MARKET_CACHE_MINUTES=30
MARKET_REQUEST_TIMEOUT=15
